!
! Copyright 1996-2017 the Authors
!
! Licensed under the EUPL, Version 1.1 only (the "Licence");
!
! You may not use this work except in compliance with the Licence.
! You may obtain a copy of the Licence at:
!   https://joinup.ec.europa.eu/software/page/eupl
!
! Unless required by applicable law or agreed to in writing,
! software distributed under the Licence is distributed on an
! "AS IS" basis, WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND,
! either express or implied.
!
! See the Licence for the specific language governing permissions
! and limitations under the Licence.

      subroutine bud_aer (dtg,kl)
!
! Description:
!   gas phase chemical reactions budget
!
! Author:
!         Roland von Glasow, Josue Bock
!
! History:
! Version   Date     Comment
! -------   ----     -------
! 1.1      08/2016   Header, implicit none                          <Josue Bock>
!                    Call in aer_drive changed to include the
!                       if(kl.eq.k) test (itself improved)
!                    Reindexed bg array for CPU efficiency
!                    Cleaning
!
! 1.0       ?        Original code.                                 <Roland von Glasow>
!
! Code Description:
!   Language:          Fortran 77 (with Fortran 90 features)
!
! Declarations:
      USE global_params, ONLY:
     &     nlev,
     &     nrxn

      implicit none

! Include statements:
      include 'aer_Parameters.h' ! KPP parameters, needed for KPP common blocks declarations
      include 'aer_Global.h'     ! KPP common blocks including rate constants and concentrations

! Subroutine arguments
! Scalar arguments with intent(in):
      double precision dtg
      integer kl

! Local scalars:
      integer i

! Common blocks:
      common /budg/ bg(2,nrxn,nlev),il(nlev)
      double precision bg ! reaction rates (bg(1,:,:): instantaneous, bg(2,:,:): cumulative) [mol/(m^3*s)]
      integer il          ! indexes of the selected levels for reaction rates output         []

!- End of header ---------------------------------------------------------------

! rates (mol/(m^3*s)
      bg(1,1,kl) = RCONST(1)*VAR(57)*FIX(1)
      bg(1,2,kl) = RCONST(2)*VAR(57)*VAR(208)
      bg(1,3,kl) = RCONST(3)*VAR(57)*FIX(3)
      bg(1,4,kl) = RCONST(4)*VAR(57)*FIX(2)
      bg(1,5,kl) = RCONST(5)*VAR(57)*VAR(91)
      bg(1,6,kl) = RCONST(6)*VAR(134)*VAR(239)
      bg(1,7,kl) = RCONST(7)*VAR(239)*FIX(1)*FIX(1)
      bg(1,8,kl) = RCONST(8)*VAR(208)*VAR(239)
      bg(1,9,kl) = RCONST(9)*VAR(239)*FIX(1)*FIX(3)
      bg(1,10,kl) = RCONST(10)*VAR(208)*VAR(231)
      bg(1,11,kl) = RCONST(11)*VAR(210)*VAR(231)
      bg(1,12,kl) = RCONST(12)*VAR(91)*VAR(231)
      bg(1,13,kl) = RCONST(13)*VAR(134)*VAR(231)
      bg(1,14,kl) = RCONST(14)*VAR(231)*VAR(231)
      bg(1,15,kl) = RCONST(15)*VAR(231)*VAR(231)
      bg(1,16,kl) = RCONST(16)*VAR(208)*VAR(210)
      bg(1,17,kl) = RCONST(17)*VAR(210)*VAR(210)
      bg(1,18,kl) = RCONST(18)*VAR(208)
      bg(1,19,kl) = RCONST(19)*VAR(208)
      bg(1,20,kl) = RCONST(20)*VAR(134)
      bg(1,21,kl) = RCONST(21)*VAR(211)*VAR(239)
      bg(1,22,kl) = RCONST(22)*VAR(211)*VAR(231)
      bg(1,23,kl) = RCONST(23)*VAR(210)*VAR(211)
      bg(1,24,kl) = RCONST(24)*VAR(208)*VAR(211)
      bg(1,25,kl) = RCONST(25)*VAR(211)*VAR(229)
      bg(1,26,kl) = RCONST(26)*VAR(239)*VAR(251)
      bg(1,27,kl) = RCONST(27)*VAR(239)*VAR(251)
      bg(1,28,kl) = RCONST(28)*VAR(231)*VAR(251)
      bg(1,29,kl) = RCONST(29)*VAR(210)*VAR(251)
      bg(1,30,kl) = RCONST(30)*VAR(208)*VAR(251)
      bg(1,31,kl) = RCONST(31)*VAR(229)*VAR(251)
      bg(1,32,kl) = RCONST(32)*VAR(251)
      bg(1,33,kl) = RCONST(33)*VAR(229)*VAR(239)
      bg(1,34,kl) = RCONST(34)*VAR(229)*VAR(231)
      bg(1,35,kl) = RCONST(35)*VAR(210)*VAR(229)
      bg(1,36,kl) = RCONST(36)*VAR(229)*VAR(229)
      bg(1,37,kl) = RCONST(37)*VAR(229)
      bg(1,38,kl) = RCONST(38)*VAR(229)
      bg(1,39,kl) = RCONST(39)*VAR(140)
      bg(1,40,kl) = RCONST(40)*VAR(140)
      bg(1,41,kl) = RCONST(41)*VAR(81)*VAR(231)
      bg(1,42,kl) = RCONST(42)*VAR(81)
      bg(1,43,kl) = RCONST(43)*VAR(187)*VAR(231)
      bg(1,44,kl) = RCONST(44)*VAR(187)
      bg(1,45,kl) = RCONST(45)*VAR(102)
      bg(1,46,kl) = RCONST(46)*VAR(102)*VAR(231)
      bg(1,47,kl) = RCONST(47)*VAR(102)
      bg(1,48,kl) = RCONST(48)*VAR(102)
      bg(1,49,kl) = RCONST(49)*VAR(77)*VAR(231)
      bg(1,50,kl) = RCONST(50)*VAR(119)*VAR(231)
      bg(1,51,kl) = RCONST(51)*VAR(43)*VAR(231)
      bg(1,52,kl) = RCONST(52)*VAR(44)*VAR(231)
      bg(1,53,kl) = RCONST(53)*VAR(143)*VAR(231)
      bg(1,54,kl) = RCONST(54)*VAR(143)*VAR(208)
      bg(1,55,kl) = RCONST(55)*VAR(143)*VAR(229)
      bg(1,56,kl) = RCONST(56)*VAR(36)*FIX(2)
      bg(1,57,kl) = RCONST(57)*VAR(207)*VAR(211)
      bg(1,58,kl) = RCONST(58)*VAR(207)*VAR(210)
      bg(1,59,kl) = RCONST(59)*VAR(207)*VAR(207)
      bg(1,60,kl) = RCONST(60)*VAR(207)*VAR(207)
      bg(1,61,kl) = RCONST(61)*VAR(128)*VAR(211)
      bg(1,62,kl) = RCONST(62)*VAR(128)*VAR(210)
      bg(1,63,kl) = RCONST(63)*VAR(128)*VAR(128)
      bg(1,64,kl) = RCONST(64)*VAR(128)*VAR(128)
      bg(1,65,kl) = RCONST(65)*VAR(128)*VAR(207)
      bg(1,66,kl) = RCONST(66)*VAR(128)*VAR(207)
      bg(1,67,kl) = RCONST(67)*VAR(128)*VAR(207)
      bg(1,68,kl) = RCONST(68)*VAR(136)*VAR(211)
      bg(1,69,kl) = RCONST(69)*VAR(136)*VAR(211)
      bg(1,70,kl) = RCONST(70)*VAR(136)*VAR(210)
      bg(1,71,kl) = RCONST(71)*VAR(136)*VAR(136)
      bg(1,72,kl) = RCONST(72)*VAR(136)*VAR(136)
      bg(1,73,kl) = RCONST(73)*VAR(136)*VAR(207)
      bg(1,74,kl) = RCONST(74)*VAR(136)*VAR(207)
      bg(1,75,kl) = RCONST(75)*VAR(136)*VAR(207)
      bg(1,76,kl) = RCONST(76)*VAR(161)*VAR(211)
      bg(1,77,kl) = RCONST(77)*VAR(161)*VAR(210)
      bg(1,78,kl) = RCONST(78)*VAR(161)*VAR(161)
      bg(1,79,kl) = RCONST(79)*VAR(161)*VAR(207)
      bg(1,80,kl) = RCONST(80)*VAR(161)*VAR(207)
      bg(1,81,kl) = RCONST(81)*VAR(161)*VAR(251)
      bg(1,82,kl) = RCONST(82)*VAR(99)
      bg(1,83,kl) = RCONST(83)*VAR(99)*VAR(231)
      bg(1,84,kl) = RCONST(84)*VAR(198)*VAR(231)
      bg(1,85,kl) = RCONST(85)*VAR(198)*VAR(229)
      bg(1,86,kl) = RCONST(86)*VAR(198)
      bg(1,87,kl) = RCONST(87)*VAR(198)
      bg(1,88,kl) = RCONST(88)*VAR(149)*VAR(231)
      bg(1,89,kl) = RCONST(89)*VAR(149)*VAR(229)
      bg(1,90,kl) = RCONST(90)*VAR(149)
      bg(1,91,kl) = RCONST(91)*VAR(184)*VAR(231)
      bg(1,92,kl) = RCONST(92)*VAR(105)*VAR(231)
      bg(1,93,kl) = RCONST(93)*VAR(105)*VAR(231)
      bg(1,94,kl) = RCONST(94)*VAR(189)*VAR(231)
      bg(1,95,kl) = RCONST(95)*VAR(88)*VAR(231)
      bg(1,96,kl) = RCONST(96)*VAR(151)*VAR(231)
      bg(1,97,kl) = RCONST(97)*VAR(151)*VAR(231)
      bg(1,98,kl) = RCONST(98)*VAR(151)
      bg(1,99,kl) = RCONST(99)*VAR(169)*VAR(239)
      bg(1,100,kl) = RCONST(100)*VAR(169)*VAR(231)
      bg(1,101,kl) = RCONST(101)*VAR(169)*VAR(208)
      bg(1,102,kl) = RCONST(102)*VAR(37)*FIX(1)
      bg(1,103,kl) = RCONST(103)*VAR(117)
      bg(1,104,kl) = RCONST(104)*VAR(77)*VAR(117)
      bg(1,105,kl) = RCONST(105)*VAR(195)*VAR(231)
      bg(1,106,kl) = RCONST(106)*VAR(195)*VAR(231)
      bg(1,107,kl) = RCONST(107)*VAR(195)*VAR(229)
      bg(1,108,kl) = RCONST(108)*VAR(195)*VAR(240)
      bg(1,109,kl) = RCONST(109)*VAR(195)*VAR(243)
      bg(1,110,kl) = RCONST(110)*VAR(195)*VAR(241)
      bg(1,111,kl) = RCONST(111)*VAR(195)*VAR(244)
      bg(1,112,kl) = RCONST(112)*VAR(195)*VAR(246)
      bg(1,113,kl) = RCONST(113)*VAR(137)*VAR(211)
      bg(1,114,kl) = RCONST(114)*VAR(137)*VAR(210)
      bg(1,115,kl) = RCONST(115)*VAR(137)*VAR(137)
      bg(1,116,kl) = RCONST(116)*VAR(137)*VAR(207)
      bg(1,117,kl) = RCONST(117)*VAR(137)*VAR(251)
      bg(1,118,kl) = RCONST(118)*VAR(111)*VAR(208)
      bg(1,119,kl) = RCONST(119)*VAR(111)*VAR(251)
      bg(1,120,kl) = RCONST(120)*VAR(111)*VAR(251)
      bg(1,121,kl) = RCONST(121)*VAR(111)*VAR(211)
      bg(1,122,kl) = RCONST(122)*VAR(111)*VAR(211)
      bg(1,123,kl) = RCONST(123)*VAR(123)*VAR(208)
      bg(1,124,kl) = RCONST(124)*VAR(123)*VAR(251)
      bg(1,125,kl) = RCONST(125)*VAR(123)*VAR(251)
      bg(1,126,kl) = RCONST(126)*VAR(152)
      bg(1,127,kl) = RCONST(127)*VAR(152)*VAR(208)
      bg(1,128,kl) = RCONST(128)*VAR(152)*VAR(251)
      bg(1,129,kl) = RCONST(129)*VAR(152)*VAR(251)
      bg(1,130,kl) = RCONST(130)*VAR(152)*VAR(211)
      bg(1,131,kl) = RCONST(131)*VAR(56)*VAR(210)
      bg(1,132,kl) = RCONST(132)*VAR(59)*VAR(231)
      bg(1,133,kl) = RCONST(133)*VAR(59)
      bg(1,134,kl) = RCONST(134)*VAR(42)
      bg(1,135,kl) = RCONST(135)*VAR(185)*VAR(231)
      bg(1,136,kl) = RCONST(136)*VAR(185)*VAR(240)
      bg(1,137,kl) = RCONST(137)*VAR(185)*VAR(244)
      bg(1,138,kl) = RCONST(138)*VAR(178)*VAR(231)
      bg(1,139,kl) = RCONST(139)*VAR(178)*VAR(229)
      bg(1,140,kl) = RCONST(140)*VAR(208)*VAR(240)
      bg(1,141,kl) = RCONST(141)*VAR(210)*VAR(240)
      bg(1,142,kl) = RCONST(142)*VAR(210)*VAR(240)
      bg(1,143,kl) = RCONST(143)*VAR(134)*VAR(240)
      bg(1,144,kl) = RCONST(144)*VAR(91)*VAR(240)
      bg(1,145,kl) = RCONST(145)*VAR(187)*VAR(240)
      bg(1,146,kl) = RCONST(146)*VAR(240)*VAR(251)
      bg(1,147,kl) = RCONST(147)*VAR(240)*VAR(251)
      bg(1,148,kl) = RCONST(148)*VAR(229)*VAR(240)
      bg(1,149,kl) = RCONST(149)*VAR(133)*VAR(231)
      bg(1,150,kl) = RCONST(150)*VAR(133)
      bg(1,151,kl) = RCONST(151)*VAR(239)*VAR(241)
      bg(1,152,kl) = RCONST(152)*VAR(231)*VAR(241)
      bg(1,153,kl) = RCONST(153)*VAR(231)*VAR(241)
      bg(1,154,kl) = RCONST(154)*VAR(210)*VAR(241)
      bg(1,155,kl) = RCONST(155)*VAR(208)*VAR(241)
      bg(1,156,kl) = RCONST(156)*VAR(208)*VAR(241)
      bg(1,157,kl) = RCONST(157)*VAR(211)*VAR(241)
      bg(1,158,kl) = RCONST(158)*VAR(241)*VAR(251)
      bg(1,159,kl) = RCONST(159)*VAR(229)*VAR(241)
      bg(1,160,kl) = RCONST(160)*VAR(229)*VAR(241)
      bg(1,161,kl) = RCONST(161)*VAR(241)*VAR(241)
      bg(1,162,kl) = RCONST(162)*VAR(241)*VAR(241)
      bg(1,163,kl) = RCONST(163)*VAR(241)*VAR(241)
      bg(1,164,kl) = RCONST(164)*VAR(241)*VAR(241)
      bg(1,165,kl) = RCONST(165)*VAR(147)*VAR(241)
      bg(1,166,kl) = RCONST(166)*VAR(147)*VAR(240)
      bg(1,167,kl) = RCONST(167)*VAR(147)*VAR(239)
      bg(1,168,kl) = RCONST(168)*VAR(147)*VAR(239)
      bg(1,169,kl) = RCONST(169)*VAR(147)*VAR(231)
      bg(1,170,kl) = RCONST(170)*VAR(147)*VAR(208)
      bg(1,171,kl) = RCONST(171)*VAR(147)*VAR(211)
      bg(1,172,kl) = RCONST(172)*VAR(147)
      bg(1,173,kl) = RCONST(173)*VAR(86)*VAR(240)
      bg(1,174,kl) = RCONST(174)*VAR(86)*VAR(208)
      bg(1,175,kl) = RCONST(175)*VAR(86)
      bg(1,176,kl) = RCONST(176)*VAR(86)
      bg(1,177,kl) = RCONST(177)*VAR(27)
      bg(1,178,kl) = RCONST(178)*VAR(163)*VAR(231)
      bg(1,179,kl) = RCONST(179)*VAR(160)*VAR(239)
      bg(1,180,kl) = RCONST(180)*VAR(160)*VAR(231)
      bg(1,181,kl) = RCONST(181)*VAR(160)
      bg(1,182,kl) = RCONST(182)*VAR(87)*VAR(231)
      bg(1,183,kl) = RCONST(183)*VAR(87)
      bg(1,184,kl) = RCONST(184)*VAR(181)*VAR(240)
      bg(1,185,kl) = RCONST(185)*VAR(181)*VAR(239)
      bg(1,186,kl) = RCONST(186)*VAR(181)*VAR(231)
      bg(1,187,kl) = RCONST(187)*VAR(181)
      bg(1,188,kl) = RCONST(188)*VAR(181)
      bg(1,189,kl) = RCONST(189)*VAR(43)*VAR(240)
      bg(1,190,kl) = RCONST(190)*VAR(44)*VAR(240)
      bg(1,191,kl) = RCONST(191)*VAR(143)*VAR(240)
      bg(1,192,kl) = RCONST(192)*VAR(198)*VAR(240)
      bg(1,193,kl) = RCONST(193)*VAR(149)*VAR(240)
      bg(1,194,kl) = RCONST(194)*VAR(184)*VAR(240)
      bg(1,195,kl) = RCONST(195)*VAR(105)*VAR(240)
      bg(1,196,kl) = RCONST(196)*VAR(151)*VAR(240)
      bg(1,197,kl) = RCONST(197)*VAR(189)*VAR(240)
      bg(1,198,kl) = RCONST(198)*VAR(88)*VAR(240)
      bg(1,199,kl) = RCONST(199)*VAR(99)*VAR(240)
      bg(1,200,kl) = RCONST(200)*VAR(207)*VAR(240)
      bg(1,201,kl) = RCONST(201)*VAR(207)*VAR(241)
      bg(1,202,kl) = RCONST(202)*VAR(54)*VAR(231)
      bg(1,203,kl) = RCONST(203)*VAR(116)*VAR(211)
      bg(1,204,kl) = RCONST(204)*VAR(116)*VAR(210)
      bg(1,205,kl) = RCONST(205)*VAR(116)*VAR(116)
      bg(1,206,kl) = RCONST(206)*VAR(116)*VAR(207)
      bg(1,207,kl) = RCONST(207)*VAR(208)*VAR(243)
      bg(1,208,kl) = RCONST(208)*VAR(210)*VAR(243)
      bg(1,209,kl) = RCONST(209)*VAR(134)*VAR(243)
      bg(1,210,kl) = RCONST(210)*VAR(243)*VAR(251)
      bg(1,211,kl) = RCONST(211)*VAR(229)*VAR(243)
      bg(1,212,kl) = RCONST(212)*VAR(221)*VAR(231)
      bg(1,213,kl) = RCONST(213)*VAR(221)*VAR(239)
      bg(1,214,kl) = RCONST(214)*VAR(221)
      bg(1,215,kl) = RCONST(215)*VAR(243)*VAR(244)
      bg(1,216,kl) = RCONST(216)*VAR(239)*VAR(244)
      bg(1,217,kl) = RCONST(217)*VAR(231)*VAR(244)
      bg(1,218,kl) = RCONST(218)*VAR(210)*VAR(244)
      bg(1,219,kl) = RCONST(219)*VAR(211)*VAR(244)
      bg(1,220,kl) = RCONST(220)*VAR(244)*VAR(251)
      bg(1,221,kl) = RCONST(221)*VAR(229)*VAR(244)
      bg(1,222,kl) = RCONST(222)*VAR(208)*VAR(244)
      bg(1,223,kl) = RCONST(223)*VAR(244)*VAR(244)
      bg(1,224,kl) = RCONST(224)*VAR(244)*VAR(244)
      bg(1,225,kl) = RCONST(225)*VAR(244)
      bg(1,226,kl) = RCONST(226)*VAR(38)*VAR(243)
      bg(1,227,kl) = RCONST(227)*VAR(164)*VAR(231)
      bg(1,228,kl) = RCONST(228)*VAR(164)*VAR(229)
      bg(1,229,kl) = RCONST(229)*VAR(150)*VAR(239)
      bg(1,230,kl) = RCONST(230)*VAR(150)
      bg(1,231,kl) = RCONST(231)*VAR(55)
      bg(1,232,kl) = RCONST(232)*VAR(180)*VAR(239)
      bg(1,233,kl) = RCONST(233)*VAR(180)*VAR(243)
      bg(1,234,kl) = RCONST(234)*VAR(180)
      bg(1,235,kl) = RCONST(235)*VAR(180)
      bg(1,236,kl) = RCONST(236)*VAR(143)*VAR(243)
      bg(1,237,kl) = RCONST(237)*VAR(198)*VAR(243)
      bg(1,238,kl) = RCONST(238)*VAR(149)*VAR(243)
      bg(1,239,kl) = RCONST(239)*VAR(151)*VAR(243)
      bg(1,240,kl) = RCONST(240)*VAR(207)*VAR(243)
      bg(1,241,kl) = RCONST(241)*VAR(198)*VAR(244)
      bg(1,242,kl) = RCONST(242)*VAR(207)*VAR(244)
      bg(1,243,kl) = RCONST(243)*VAR(207)*VAR(244)
      bg(1,244,kl) = RCONST(244)*VAR(39)*VAR(231)
      bg(1,245,kl) = RCONST(245)*VAR(120)*VAR(211)
      bg(1,246,kl) = RCONST(246)*VAR(120)*VAR(210)
      bg(1,247,kl) = RCONST(247)*VAR(120)*VAR(120)
      bg(1,248,kl) = RCONST(248)*VAR(120)*VAR(207)
      bg(1,249,kl) = RCONST(249)*VAR(208)*VAR(245)
      bg(1,250,kl) = RCONST(250)*VAR(210)*VAR(245)
      bg(1,251,kl) = RCONST(251)*VAR(245)*VAR(251)
      bg(1,252,kl) = RCONST(252)*VAR(229)*VAR(245)
      bg(1,253,kl) = RCONST(253)*VAR(245)*VAR(245)
      bg(1,254,kl) = RCONST(254)*VAR(175)*VAR(239)
      bg(1,255,kl) = RCONST(255)*VAR(175)*VAR(231)
      bg(1,256,kl) = RCONST(256)*VAR(175)*VAR(229)
      bg(1,257,kl) = RCONST(257)*VAR(175)
      bg(1,258,kl) = RCONST(258)*VAR(239)*VAR(246)
      bg(1,259,kl) = RCONST(259)*VAR(210)*VAR(246)
      bg(1,260,kl) = RCONST(260)*VAR(211)*VAR(246)
      bg(1,261,kl) = RCONST(261)*VAR(246)*VAR(251)
      bg(1,262,kl) = RCONST(262)*VAR(246)*VAR(246)
      bg(1,263,kl) = RCONST(263)*VAR(246)
      bg(1,264,kl) = RCONST(264)*VAR(138)*VAR(231)
      bg(1,265,kl) = RCONST(265)*VAR(138)*VAR(211)
      bg(1,266,kl) = RCONST(266)*VAR(138)*VAR(138)
      bg(1,267,kl) = RCONST(267)*VAR(138)
      bg(1,268,kl) = RCONST(268)*VAR(53)
      bg(1,269,kl) = RCONST(269)*VAR(109)*VAR(231)
      bg(1,270,kl) = RCONST(270)*VAR(109)*VAR(229)
      bg(1,271,kl) = RCONST(271)*VAR(98)
      bg(1,272,kl) = RCONST(272)*VAR(65)*VAR(65)
      bg(1,273,kl) = RCONST(273)*VAR(65)
      bg(1,274,kl) = RCONST(274)*VAR(118)
      bg(1,275,kl) = RCONST(275)*VAR(118)
      bg(1,276,kl) = RCONST(276)*VAR(1)
      bg(1,277,kl) = RCONST(277)*VAR(69)*VAR(231)
      bg(1,278,kl) = RCONST(278)*VAR(69)
      bg(1,279,kl) = RCONST(279)*VAR(66)*VAR(231)
      bg(1,280,kl) = RCONST(280)*VAR(66)
      bg(1,281,kl) = RCONST(281)*VAR(75)*VAR(231)
      bg(1,282,kl) = RCONST(282)*VAR(75)
      bg(1,283,kl) = RCONST(283)*VAR(207)*VAR(246)
      bg(1,284,kl) = RCONST(284)*VAR(108)*VAR(211)
      bg(1,285,kl) = RCONST(285)*VAR(108)*VAR(210)
      bg(1,286,kl) = RCONST(286)*VAR(108)*VAR(108)
      bg(1,287,kl) = RCONST(287)*VAR(108)*VAR(207)
      bg(1,288,kl) = RCONST(288)*VAR(202)*VAR(240)
      bg(1,289,kl) = RCONST(289)*VAR(221)*VAR(240)
      bg(1,290,kl) = RCONST(290)*VAR(175)*VAR(240)
      bg(1,291,kl) = RCONST(291)*VAR(240)*VAR(246)
      bg(1,292,kl) = RCONST(292)*VAR(147)*VAR(243)
      bg(1,293,kl) = RCONST(293)*VAR(133)*VAR(243)
      bg(1,294,kl) = RCONST(294)*VAR(86)*VAR(243)
      bg(1,295,kl) = RCONST(295)*VAR(202)*VAR(243)
      bg(1,296,kl) = RCONST(296)*VAR(175)*VAR(243)
      bg(1,297,kl) = RCONST(297)*VAR(243)*VAR(246)
      bg(1,298,kl) = RCONST(298)*VAR(135)*VAR(243)
      bg(1,299,kl) = RCONST(299)*VAR(221)*VAR(245)
      bg(1,300,kl) = RCONST(300)*VAR(244)*VAR(245)
      bg(1,301,kl) = RCONST(301)*VAR(175)*VAR(244)
      bg(1,302,kl) = RCONST(302)*VAR(241)*VAR(244)
      bg(1,303,kl) = RCONST(303)*VAR(241)*VAR(244)
      bg(1,304,kl) = RCONST(304)*VAR(241)*VAR(244)
      bg(1,305,kl) = RCONST(305)*VAR(241)*VAR(246)
      bg(1,306,kl) = RCONST(306)*VAR(241)*VAR(246)
      bg(1,307,kl) = RCONST(307)*VAR(241)*VAR(246)
      bg(1,308,kl) = RCONST(308)*VAR(244)*VAR(246)
      bg(1,309,kl) = RCONST(309)*VAR(244)*VAR(246)
      bg(1,310,kl) = RCONST(310)*VAR(135)*VAR(231)
      bg(1,311,kl) = RCONST(311)*VAR(135)
      bg(1,312,kl) = RCONST(312)*VAR(243)*VAR(252)
      bg(1,313,kl) = RCONST(313)*VAR(240)*VAR(252)
      bg(1,314,kl) = RCONST(314)*VAR(231)*VAR(252)
      bg(1,315,kl) = RCONST(315)*VAR(252)
      bg(1,316,kl) = RCONST(316)*VAR(202)*VAR(239)
      bg(1,317,kl) = RCONST(317)*VAR(202)*VAR(231)
      bg(1,318,kl) = RCONST(318)*VAR(202)
      bg(1,319,kl) = RCONST(319)*VAR(69)*VAR(240)
      bg(1,320,kl) = RCONST(320)*VAR(66)*VAR(240)
      bg(1,321,kl) = RCONST(321)*VAR(75)*VAR(240)
      bg(1,322,kl) = RCONST(322)*VAR(3)
      bg(1,323,kl) = RCONST(323)*VAR(2)
      bg(1,324,kl) = RCONST(324)*VAR(187)
      bg(1,325,kl) = RCONST(325)*VAR(140)
      bg(1,326,kl) = RCONST(326)*VAR(77)
      bg(1,327,kl) = RCONST(327)*VAR(40)
      bg(1,328,kl) = RCONST(328)*VAR(187)
      bg(1,329,kl) = RCONST(329)*VAR(140)
      bg(1,330,kl) = RCONST(330)*VAR(77)
      bg(1,331,kl) = RCONST(331)*VAR(40)
      bg(1,332,kl) = RCONST(332)*VAR(140)
      bg(1,333,kl) = RCONST(333)*VAR(140)*VAR(253)
      bg(1,334,kl) = RCONST(334)*VAR(140)*VAR(217)
      bg(1,335,kl) = RCONST(335)*VAR(181)
      bg(1,336,kl) = RCONST(336)*VAR(181)*VAR(253)
      bg(1,337,kl) = RCONST(337)*VAR(181)*VAR(217)
      bg(1,338,kl) = RCONST(338)*VAR(180)
      bg(1,339,kl) = RCONST(339)*VAR(180)*VAR(253)
      bg(1,340,kl) = RCONST(340)*VAR(180)*VAR(217)
      bg(1,341,kl) = RCONST(341)*VAR(118)
      bg(1,342,kl) = RCONST(342)*VAR(109)
      bg(1,343,kl) = RCONST(343)*VAR(53)
      bg(1,344,kl) = RCONST(344)*VAR(65)
      bg(1,345,kl) = RCONST(345)*VAR(138)
      bg(1,346,kl) = RCONST(346)*VAR(41)
      bg(1,347,kl) = RCONST(347)*VAR(140)
      bg(1,348,kl) = RCONST(348)*VAR(140)*VAR(227)
      bg(1,349,kl) = RCONST(349)*VAR(140)*VAR(236)
      bg(1,350,kl) = RCONST(350)*VAR(181)
      bg(1,351,kl) = RCONST(351)*VAR(181)*VAR(227)
      bg(1,352,kl) = RCONST(352)*VAR(181)*VAR(236)
      bg(1,353,kl) = RCONST(353)*VAR(180)
      bg(1,354,kl) = RCONST(354)*VAR(180)*VAR(227)
      bg(1,355,kl) = RCONST(355)*VAR(180)*VAR(236)
      bg(1,356,kl) = RCONST(356)*VAR(118)
      bg(1,357,kl) = RCONST(357)*VAR(109)
      bg(1,358,kl) = RCONST(358)*VAR(53)
      bg(1,359,kl) = RCONST(359)*VAR(65)
      bg(1,360,kl) = RCONST(360)*VAR(138)
      bg(1,361,kl) = RCONST(361)*VAR(41)
      bg(1,362,kl) = RCONST(362)*VAR(224)*VAR(250)
      bg(1,363,kl) = RCONST(363)*VAR(224)*VAR(238)
      bg(1,364,kl) = RCONST(364)*VAR(250)*VAR(250)
      bg(1,365,kl) = RCONST(365)*VAR(214)*VAR(250)
      bg(1,366,kl) = RCONST(366)*VAR(238)*VAR(250)
      bg(1,367,kl) = RCONST(367)*VAR(232)*VAR(250)
      bg(1,368,kl) = RCONST(368)*VAR(214)*VAR(214)
      bg(1,369,kl) = RCONST(369)*VAR(214)*VAR(238)
      bg(1,370,kl) = RCONST(370)*VAR(182)*VAR(250)
      bg(1,371,kl) = RCONST(371)*VAR(182)*VAR(232)*VAR(248)
      bg(1,372,kl) = RCONST(372)*VAR(155)*VAR(255)
      bg(1,373,kl) = RCONST(373)*VAR(162)*VAR(162)
      bg(1,374,kl) = RCONST(374)*VAR(162)*VAR(214)
      bg(1,375,kl) = RCONST(375)*VAR(142)*VAR(224)
      bg(1,376,kl) = RCONST(376)*VAR(142)*VAR(250)
      bg(1,377,kl) = RCONST(377)*VAR(45)
      bg(1,378,kl) = RCONST(378)*VAR(159)*VAR(250)
      bg(1,379,kl) = RCONST(379)*VAR(110)*VAR(250)
      bg(1,380,kl) = RCONST(380)*VAR(97)*VAR(250)
      bg(1,381,kl) = RCONST(381)*VAR(125)*VAR(214)
      bg(1,382,kl) = RCONST(382)*VAR(125)*VAR(238)
      bg(1,383,kl) = RCONST(383)*VAR(85)*VAR(250)
      bg(1,384,kl) = RCONST(384)*VAR(203)*VAR(250)
      bg(1,385,kl) = RCONST(385)*VAR(203)*VAR(250)
      bg(1,386,kl) = RCONST(386)*VAR(126)*VAR(238)
      bg(1,387,kl) = RCONST(387)*VAR(126)*VAR(232)
      bg(1,388,kl) = RCONST(388)*VAR(97)*VAR(126)
      bg(1,389,kl) = RCONST(389)*VAR(96)*VAR(250)
      bg(1,390,kl) = RCONST(390)*VAR(114)*VAR(250)
      bg(1,391,kl) = RCONST(391)*VAR(78)*VAR(82)
      bg(1,392,kl) = RCONST(392)*VAR(218)*VAR(224)
      bg(1,393,kl) = RCONST(393)*VAR(224)*VAR(226)
      bg(1,394,kl) = RCONST(394)*VAR(218)*VAR(250)
      bg(1,395,kl) = RCONST(395)*VAR(226)*VAR(250)
      bg(1,396,kl) = RCONST(396)*VAR(214)*VAR(218)
      bg(1,397,kl) = RCONST(397)*VAR(218)*VAR(238)
      bg(1,398,kl) = RCONST(398)*VAR(218)*VAR(232)
      bg(1,399,kl) = RCONST(399)*VAR(162)*VAR(218)
      bg(1,400,kl) = RCONST(400)*VAR(162)*VAR(226)
      bg(1,401,kl) = RCONST(401)*VAR(155)*VAR(218)
      bg(1,402,kl) = RCONST(402)*VAR(201)*VAR(218)
      bg(1,403,kl) = RCONST(403)*VAR(203)*VAR(218)*VAR(248)
      bg(1,404,kl) = RCONST(404)*VAR(203)*VAR(226)*VAR(248)
      bg(1,405,kl) = RCONST(405)*VAR(159)*VAR(218)
      bg(1,406,kl) = RCONST(406)*VAR(159)*VAR(226)
      bg(1,407,kl) = RCONST(407)*VAR(61)*VAR(255)
      bg(1,408,kl) = RCONST(408)*VAR(129)*VAR(218)*VAR(248)
      bg(1,409,kl) = RCONST(409)*VAR(176)*VAR(250)
      bg(1,410,kl) = RCONST(410)*VAR(176)*VAR(214)
      bg(1,411,kl) = RCONST(411)*VAR(176)*VAR(238)
      bg(1,412,kl) = RCONST(412)*VAR(176)*FIX(4)
      bg(1,413,kl) = RCONST(413)*VAR(176)*VAR(232)
      bg(1,414,kl) = RCONST(414)*VAR(176)*VAR(249)
      bg(1,415,kl) = RCONST(415)*VAR(176)*VAR(218)
      bg(1,416,kl) = RCONST(416)*VAR(176)*VAR(226)
      bg(1,417,kl) = RCONST(417)*VAR(155)*VAR(199)
      bg(1,418,kl) = RCONST(418)*VAR(193)*VAR(218)
      bg(1,419,kl) = RCONST(419)*VAR(193)*VAR(226)
      bg(1,420,kl) = RCONST(420)*VAR(193)*VAR(218)
      bg(1,421,kl) = RCONST(421)*VAR(193)*VAR(226)
      bg(1,422,kl) = RCONST(422)*VAR(193)*VAR(238)
      bg(1,423,kl) = RCONST(423)*VAR(193)*VAR(193)
      bg(1,424,kl) = RCONST(424)*VAR(148)*VAR(224)
      bg(1,425,kl) = RCONST(425)*VAR(148)*VAR(250)
      bg(1,426,kl) = RCONST(426)*VAR(84)*VAR(250)
      bg(1,427,kl) = RCONST(427)*VAR(62)*VAR(250)
      bg(1,428,kl) = RCONST(428)*VAR(179)*VAR(250)
      bg(1,429,kl) = RCONST(429)*VAR(191)*VAR(232)
      bg(1,430,kl) = RCONST(430)*VAR(191)*FIX(4)
      bg(1,431,kl) = RCONST(431)*VAR(191)*VAR(249)
      bg(1,432,kl) = RCONST(432)*VAR(114)*VAR(191)
      bg(1,433,kl) = RCONST(433)*VAR(191)*VAR(199)
      bg(1,434,kl) = RCONST(434)*VAR(191)*VAR(191)
      bg(1,435,kl) = RCONST(435)*VAR(250)*VAR(253)
      bg(1,436,kl) = RCONST(436)*VAR(224)*VAR(253)
      bg(1,437,kl) = RCONST(437)*VAR(155)*VAR(253)
      bg(1,438,kl) = RCONST(438)*VAR(201)*VAR(253)
      bg(1,439,kl) = RCONST(439)*VAR(176)*VAR(253)
      bg(1,440,kl) = RCONST(440)*VAR(129)*VAR(253)
      bg(1,441,kl) = RCONST(441)*VAR(223)*VAR(248)*VAR(253)
      bg(1,442,kl) = RCONST(442)*VAR(216)
      bg(1,443,kl) = RCONST(443)*VAR(220)*VAR(250)
      bg(1,444,kl) = RCONST(444)*VAR(220)*VAR(255)
      bg(1,445,kl) = RCONST(445)*VAR(214)*VAR(220)
      bg(1,446,kl) = RCONST(446)*VAR(220)*VAR(238)
      bg(1,447,kl) = RCONST(447)*VAR(220)*VAR(232)
      bg(1,448,kl) = RCONST(448)*VAR(142)*VAR(220)
      bg(1,449,kl) = RCONST(449)*VAR(203)*VAR(220)
      bg(1,450,kl) = RCONST(450)*VAR(114)*VAR(220)
      bg(1,451,kl) = RCONST(451)*VAR(218)*VAR(220)
      bg(1,452,kl) = RCONST(452)*VAR(220)*VAR(226)
      bg(1,453,kl) = RCONST(453)*VAR(220)*VAR(220)
      bg(1,454,kl) = RCONST(454)*VAR(191)*VAR(220)
      bg(1,455,kl) = RCONST(455)*VAR(148)*VAR(220)
      bg(1,456,kl) = RCONST(456)*VAR(70)
      bg(1,457,kl) = RCONST(457)*VAR(70)*VAR(248)
      bg(1,458,kl) = RCONST(458)*VAR(214)*VAR(223)
      bg(1,459,kl) = RCONST(459)*VAR(223)*VAR(238)
      bg(1,460,kl) = RCONST(460)*VAR(223)*VAR(226)
      bg(1,461,kl) = RCONST(461)*VAR(218)*VAR(223)
      bg(1,462,kl) = RCONST(462)*VAR(214)*VAR(216)
      bg(1,463,kl) = RCONST(463)*VAR(216)*VAR(238)
      bg(1,464,kl) = RCONST(464)*VAR(171)*VAR(255)
      bg(1,465,kl) = RCONST(465)*VAR(114)*VAR(171)
      bg(1,466,kl) = RCONST(466)*VAR(217)*VAR(250)
      bg(1,467,kl) = RCONST(467)*VAR(217)*VAR(224)
      bg(1,468,kl) = RCONST(468)*VAR(155)*VAR(217)
      bg(1,469,kl) = RCONST(469)*VAR(201)*VAR(217)
      bg(1,470,kl) = RCONST(470)*VAR(176)*VAR(217)
      bg(1,471,kl) = RCONST(471)*VAR(129)*VAR(217)
      bg(1,472,kl) = RCONST(472)*VAR(217)*VAR(222)*VAR(248)
      bg(1,473,kl) = RCONST(473)*VAR(168)
      bg(1,474,kl) = RCONST(474)*VAR(235)*VAR(238)
      bg(1,475,kl) = RCONST(475)*VAR(214)*VAR(235)
      bg(1,476,kl) = RCONST(476)*VAR(232)*VAR(235)
      bg(1,477,kl) = RCONST(477)*VAR(235)*VAR(235)
      bg(1,478,kl) = RCONST(478)*VAR(203)*VAR(235)
      bg(1,479,kl) = RCONST(479)*VAR(114)*VAR(235)
      bg(1,480,kl) = RCONST(480)*VAR(142)*VAR(235)
      bg(1,481,kl) = RCONST(481)*VAR(218)*VAR(235)
      bg(1,482,kl) = RCONST(482)*VAR(226)*VAR(235)
      bg(1,483,kl) = RCONST(483)*VAR(148)*VAR(235)
      bg(1,484,kl) = RCONST(484)*VAR(103)
      bg(1,485,kl) = RCONST(485)*VAR(103)
      bg(1,486,kl) = RCONST(486)*VAR(103)*VAR(248)
      bg(1,487,kl) = RCONST(487)*VAR(103)*VAR(217)
      bg(1,488,kl) = RCONST(488)*VAR(93)*VAR(226)
      bg(1,489,kl) = RCONST(489)*VAR(214)*VAR(222)
      bg(1,490,kl) = RCONST(490)*VAR(222)*VAR(238)
      bg(1,491,kl) = RCONST(491)*VAR(222)*VAR(232)
      bg(1,492,kl) = RCONST(492)*VAR(222)*VAR(226)
      bg(1,493,kl) = RCONST(493)*VAR(218)*VAR(222)
      bg(1,494,kl) = RCONST(494)*VAR(168)*VAR(214)
      bg(1,495,kl) = RCONST(495)*VAR(168)*VAR(238)
      bg(1,496,kl) = RCONST(496)*VAR(174)*VAR(194)*VAR(248)
      bg(1,497,kl) = RCONST(497)*VAR(196)
      bg(1,498,kl) = RCONST(498)*VAR(194)*VAR(248)*VAR(253)
      bg(1,499,kl) = RCONST(499)*VAR(131)
      bg(1,500,kl) = RCONST(500)*VAR(194)*VAR(217)*VAR(248)
      bg(1,501,kl) = RCONST(501)*VAR(188)
      bg(1,502,kl) = RCONST(502)*VAR(174)*VAR(223)*VAR(248)
      bg(1,503,kl) = RCONST(503)*VAR(174)*VAR(222)*VAR(248)
      bg(1,504,kl) = RCONST(504)*VAR(25)*VAR(25)
      bg(1,505,kl) = RCONST(505)*VAR(174)*VAR(224)
      bg(1,506,kl) = RCONST(506)*VAR(174)*VAR(188)
      bg(1,507,kl) = RCONST(507)*VAR(196)*VAR(217)
      bg(1,508,kl) = RCONST(508)*VAR(131)*VAR(174)
      bg(1,509,kl) = RCONST(509)*VAR(194)*VAR(216)
      bg(1,510,kl) = RCONST(510)*VAR(194)*VAR(218)
      bg(1,511,kl) = RCONST(511)*VAR(194)*VAR(226)
      bg(1,512,kl) = RCONST(512)*VAR(194)*VAR(223)
      bg(1,513,kl) = RCONST(513)*VAR(194)*VAR(222)
      bg(1,514,kl) = RCONST(514)*VAR(194)*VAR(194)
      bg(1,515,kl) = RCONST(515)*VAR(156)*VAR(174)*VAR(248)
      bg(1,516,kl) = RCONST(516)*VAR(156)*VAR(232)
      bg(1,517,kl) = RCONST(517)*VAR(156)*VAR(223)
      bg(1,518,kl) = RCONST(518)*VAR(156)*VAR(222)
      bg(1,519,kl) = RCONST(519)*VAR(156)*VAR(194)
      bg(1,520,kl) = RCONST(520)*VAR(121)*VAR(174)*VAR(248)
      bg(1,521,kl) = RCONST(521)*VAR(156)*VAR(196)
      bg(1,522,kl) = RCONST(522)*VAR(121)*VAR(174)*VAR(174)
      bg(1,523,kl) = RCONST(523)*VAR(196)*VAR(218)
      bg(1,524,kl) = RCONST(524)*VAR(217)*VAR(223)*VAR(248)
      bg(1,525,kl) = RCONST(525)*VAR(222)*VAR(248)*VAR(253)
      bg(1,526,kl) = RCONST(526)*VAR(146)
      bg(1,527,kl) = RCONST(527)*VAR(92)*VAR(217)*VAR(248)
      bg(1,528,kl) = RCONST(528)*VAR(216)*VAR(217)
      bg(1,529,kl) = RCONST(529)*VAR(67)
      bg(1,530,kl) = RCONST(530)*VAR(224)
      bg(1,531,kl) = RCONST(531)*VAR(232)
      bg(1,532,kl) = RCONST(532)*VAR(249)
      bg(1,533,kl) = RCONST(533)*VAR(142)
      bg(1,534,kl) = RCONST(534)*VAR(223)
      bg(1,535,kl) = RCONST(535)*VAR(216)
      bg(1,536,kl) = RCONST(536)*VAR(222)
      bg(1,537,kl) = RCONST(537)*VAR(168)
      bg(1,538,kl) = RCONST(538)*VAR(146)
      bg(1,539,kl) = RCONST(539)*FIX(1)
      bg(1,540,kl) = RCONST(540)*VAR(82)
      bg(1,541,kl) = RCONST(541)*VAR(208)
      bg(1,542,kl) = RCONST(542)*VAR(224)
      bg(1,543,kl) = RCONST(543)*VAR(231)
      bg(1,544,kl) = RCONST(544)*VAR(250)
      bg(1,545,kl) = RCONST(545)*VAR(210)
      bg(1,546,kl) = RCONST(546)*VAR(214)
      bg(1,547,kl) = RCONST(547)*VAR(134)
      bg(1,548,kl) = RCONST(548)*VAR(232)
      bg(1,549,kl) = RCONST(549)*VAR(58)
      bg(1,550,kl) = RCONST(550)*VAR(186)
      bg(1,551,kl) = RCONST(551)*VAR(207)
      bg(1,552,kl) = RCONST(552)*VAR(125)
      bg(1,553,kl) = RCONST(553)*VAR(198)
      bg(1,554,kl) = RCONST(554)*VAR(159)
      bg(1,555,kl) = RCONST(555)*VAR(189)
      bg(1,556,kl) = RCONST(556)*VAR(110)
      bg(1,557,kl) = RCONST(557)*VAR(88)
      bg(1,558,kl) = RCONST(558)*VAR(11)
      bg(1,559,kl) = RCONST(559)*VAR(184)
      bg(1,560,kl) = RCONST(560)*VAR(85)
      bg(1,561,kl) = RCONST(561)*VAR(105)
      bg(1,562,kl) = RCONST(562)*VAR(13)
      bg(1,563,kl) = RCONST(563)*VAR(151)
      bg(1,564,kl) = RCONST(564)*VAR(203)
      bg(1,565,kl) = RCONST(565)*VAR(211)
      bg(1,566,kl) = RCONST(566)*VAR(23)
      bg(1,567,kl) = RCONST(567)*VAR(251)
      bg(1,568,kl) = RCONST(568)*VAR(162)
      bg(1,569,kl) = RCONST(569)*VAR(229)
      bg(1,570,kl) = RCONST(570)*VAR(155)
      bg(1,571,kl) = RCONST(571)*VAR(81)
      bg(1,572,kl) = RCONST(572)*VAR(182)
      bg(1,573,kl) = RCONST(573)*VAR(187)
      bg(1,574,kl) = RCONST(574)*VAR(107)
      bg(1,575,kl) = RCONST(575)*VAR(102)
      bg(1,576,kl) = RCONST(576)*VAR(201)
      bg(1,577,kl) = RCONST(577)*VAR(77)
      bg(1,578,kl) = RCONST(578)*VAR(29)
      bg(1,579,kl) = RCONST(579)*VAR(169)
      bg(1,580,kl) = RCONST(580)*VAR(47)
      bg(1,581,kl) = RCONST(581)*VAR(40)
      bg(1,582,kl) = RCONST(582)*VAR(60)
      bg(1,583,kl) = RCONST(583)*VAR(195)
      bg(1,584,kl) = RCONST(584)*VAR(148)
      bg(1,585,kl) = RCONST(585)*VAR(185)
      bg(1,586,kl) = RCONST(586)*VAR(84)
      bg(1,587,kl) = RCONST(587)*VAR(18)
      bg(1,588,kl) = RCONST(588)*VAR(12)
      bg(1,589,kl) = RCONST(589)*VAR(178)
      bg(1,590,kl) = RCONST(590)*VAR(62)
      bg(1,591,kl) = RCONST(591)*VAR(113)
      bg(1,592,kl) = RCONST(592)*VAR(179)
      bg(1,593,kl) = RCONST(593)*VAR(59)
      bg(1,594,kl) = RCONST(594)*VAR(15)
      bg(1,595,kl) = RCONST(595)*VAR(163)
      bg(1,596,kl) = RCONST(596)*VAR(48)
      bg(1,597,kl) = RCONST(597)*VAR(160)
      bg(1,598,kl) = RCONST(598)*VAR(223)
      bg(1,599,kl) = RCONST(599)*VAR(133)
      bg(1,600,kl) = RCONST(600)*VAR(216)
      bg(1,601,kl) = RCONST(601)*VAR(164)
      bg(1,602,kl) = RCONST(602)*VAR(49)
      bg(1,603,kl) = RCONST(603)*VAR(150)
      bg(1,604,kl) = RCONST(604)*VAR(222)
      bg(1,605,kl) = RCONST(605)*VAR(221)
      bg(1,606,kl) = RCONST(606)*VAR(168)
      bg(1,607,kl) = RCONST(607)*VAR(202)
      bg(1,608,kl) = RCONST(608)*VAR(146)
      bg(1,609,kl) = RCONST(609)*VAR(98)
      bg(1,610,kl) = RCONST(610)*VAR(194)
      bg(1,611,kl) = RCONST(611)*VAR(246)
      bg(1,612,kl) = RCONST(612)*VAR(25)
      bg(1,613,kl) = RCONST(613)*VAR(175)
      bg(1,614,kl) = RCONST(614)*VAR(196)
      bg(1,615,kl) = RCONST(615)*VAR(252)
      bg(1,616,kl) = RCONST(616)*VAR(131)
      bg(1,617,kl) = RCONST(617)*VAR(135)
      bg(1,618,kl) = RCONST(618)*VAR(188)
      bg(1,619,kl) = RCONST(619)*VAR(21)
      bg(1,620,kl) = RCONST(620)*VAR(14)
      bg(1,621,kl) = RCONST(621)*FIX(4)
      bg(1,622,kl) = RCONST(622)*VAR(248)*VAR(255)
      bg(1,623,kl) = RCONST(623)*VAR(214)
      bg(1,624,kl) = RCONST(624)*VAR(238)*VAR(248)
      bg(1,625,kl) = RCONST(625)*VAR(110)
      bg(1,626,kl) = RCONST(626)*VAR(97)*VAR(248)
      bg(1,627,kl) = RCONST(627)*VAR(186)
      bg(1,628,kl) = RCONST(628)*VAR(96)*VAR(248)
      bg(1,629,kl) = RCONST(629)*VAR(182)
      bg(1,630,kl) = RCONST(630)*VAR(142)*VAR(248)
      bg(1,631,kl) = RCONST(631)*VAR(107)
      bg(1,632,kl) = RCONST(632)*VAR(248)*VAR(249)
      bg(1,633,kl) = RCONST(633)*VAR(201)
      bg(1,634,kl) = RCONST(634)*VAR(45)*VAR(248)
      bg(1,635,kl) = RCONST(635)*VAR(29)
      bg(1,636,kl) = RCONST(636)*VAR(28)*VAR(255)
      bg(1,637,kl) = RCONST(637)*VAR(218)
      bg(1,638,kl) = RCONST(638)*VAR(226)*VAR(248)
      bg(1,639,kl) = RCONST(639)*VAR(60)
      bg(1,640,kl) = RCONST(640)*VAR(139)*VAR(248)
      bg(1,641,kl) = RCONST(641)*VAR(139)
      bg(1,642,kl) = RCONST(642)*VAR(199)*VAR(248)
      bg(1,643,kl) = RCONST(643)*VAR(47)
      bg(1,644,kl) = RCONST(644)*VAR(218)*VAR(248)
      bg(1,645,kl) = RCONST(645)*VAR(48)
      bg(1,646,kl) = RCONST(646)*VAR(248)*VAR(253)
      bg(1,647,kl) = RCONST(647)*VAR(220)
      bg(1,648,kl) = RCONST(648)*VAR(191)*VAR(253)
      bg(1,649,kl) = RCONST(649)*VAR(223)
      bg(1,650,kl) = RCONST(650)*VAR(92)*VAR(248)
      bg(1,651,kl) = RCONST(651)*VAR(49)
      bg(1,652,kl) = RCONST(652)*VAR(217)*VAR(248)
      bg(1,653,kl) = RCONST(653)*VAR(235)
      bg(1,654,kl) = RCONST(654)*VAR(171)*VAR(217)
      bg(1,655,kl) = RCONST(655)*VAR(222)
      bg(1,656,kl) = RCONST(656)*VAR(93)*VAR(248)
      bg(1,657,kl) = RCONST(657)*VAR(146)*VAR(253)
      bg(1,658,kl) = RCONST(658)*VAR(67)
      bg(1,659,kl) = RCONST(659)*VAR(146)*VAR(217)
      bg(1,660,kl) = RCONST(660)*VAR(68)
      bg(1,661,kl) = RCONST(661)*VAR(168)*VAR(253)
      bg(1,662,kl) = RCONST(662)*VAR(68)
      bg(1,663,kl) = RCONST(663)*VAR(131)*VAR(253)
      bg(1,664,kl) = RCONST(664)*VAR(30)
      bg(1,665,kl) = RCONST(665)*VAR(188)*VAR(217)
      bg(1,666,kl) = RCONST(666)*VAR(31)
      bg(1,667,kl) = RCONST(667)*VAR(131)*VAR(217)
      bg(1,668,kl) = RCONST(668)*VAR(71)
      bg(1,669,kl) = RCONST(669)*VAR(188)*VAR(253)
      bg(1,670,kl) = RCONST(670)*VAR(71)
      bg(1,671,kl) = RCONST(671)*VAR(213)*VAR(219)
      bg(1,672,kl) = RCONST(672)*VAR(213)*VAR(257)
      bg(1,673,kl) = RCONST(673)*VAR(219)*VAR(219)
      bg(1,674,kl) = RCONST(674)*VAR(219)*VAR(247)
      bg(1,675,kl) = RCONST(675)*VAR(219)*VAR(257)
      bg(1,676,kl) = RCONST(676)*VAR(219)*VAR(233)
      bg(1,677,kl) = RCONST(677)*VAR(247)*VAR(247)
      bg(1,678,kl) = RCONST(678)*VAR(247)*VAR(257)
      bg(1,679,kl) = RCONST(679)*VAR(200)*VAR(219)
      bg(1,680,kl) = RCONST(680)*VAR(200)*VAR(230)*VAR(233)
      bg(1,681,kl) = RCONST(681)*VAR(153)*VAR(228)
      bg(1,682,kl) = RCONST(682)*VAR(158)*VAR(158)
      bg(1,683,kl) = RCONST(683)*VAR(158)*VAR(247)
      bg(1,684,kl) = RCONST(684)*VAR(141)*VAR(213)
      bg(1,685,kl) = RCONST(685)*VAR(141)*VAR(219)
      bg(1,686,kl) = RCONST(686)*VAR(46)
      bg(1,687,kl) = RCONST(687)*VAR(157)*VAR(219)
      bg(1,688,kl) = RCONST(688)*VAR(112)*VAR(219)
      bg(1,689,kl) = RCONST(689)*VAR(101)*VAR(219)
      bg(1,690,kl) = RCONST(690)*VAR(124)*VAR(247)
      bg(1,691,kl) = RCONST(691)*VAR(124)*VAR(257)
      bg(1,692,kl) = RCONST(692)*VAR(90)*VAR(219)
      bg(1,693,kl) = RCONST(693)*VAR(197)*VAR(219)
      bg(1,694,kl) = RCONST(694)*VAR(197)*VAR(219)
      bg(1,695,kl) = RCONST(695)*VAR(127)*VAR(257)
      bg(1,696,kl) = RCONST(696)*VAR(127)*VAR(233)
      bg(1,697,kl) = RCONST(697)*VAR(101)*VAR(127)
      bg(1,698,kl) = RCONST(698)*VAR(100)*VAR(219)
      bg(1,699,kl) = RCONST(699)*VAR(115)*VAR(219)
      bg(1,700,kl) = RCONST(700)*VAR(79)*VAR(83)
      bg(1,701,kl) = RCONST(701)*VAR(213)*VAR(237)
      bg(1,702,kl) = RCONST(702)*VAR(212)*VAR(213)
      bg(1,703,kl) = RCONST(703)*VAR(219)*VAR(237)
      bg(1,704,kl) = RCONST(704)*VAR(212)*VAR(219)
      bg(1,705,kl) = RCONST(705)*VAR(237)*VAR(247)
      bg(1,706,kl) = RCONST(706)*VAR(237)*VAR(257)
      bg(1,707,kl) = RCONST(707)*VAR(233)*VAR(237)
      bg(1,708,kl) = RCONST(708)*VAR(158)*VAR(237)
      bg(1,709,kl) = RCONST(709)*VAR(158)*VAR(212)
      bg(1,710,kl) = RCONST(710)*VAR(153)*VAR(237)
      bg(1,711,kl) = RCONST(711)*VAR(183)*VAR(237)
      bg(1,712,kl) = RCONST(712)*VAR(197)*VAR(230)*VAR(237)
      bg(1,713,kl) = RCONST(713)*VAR(197)*VAR(212)*VAR(230)
      bg(1,714,kl) = RCONST(714)*VAR(157)*VAR(237)
      bg(1,715,kl) = RCONST(715)*VAR(157)*VAR(212)
      bg(1,716,kl) = RCONST(716)*VAR(63)*VAR(228)
      bg(1,717,kl) = RCONST(717)*VAR(130)*VAR(230)*VAR(237)
      bg(1,718,kl) = RCONST(718)*VAR(177)*VAR(219)
      bg(1,719,kl) = RCONST(719)*VAR(177)*VAR(247)
      bg(1,720,kl) = RCONST(720)*VAR(177)*VAR(257)
      bg(1,721,kl) = RCONST(721)*VAR(177)*FIX(5)
      bg(1,722,kl) = RCONST(722)*VAR(177)*VAR(233)
      bg(1,723,kl) = RCONST(723)*VAR(177)*VAR(242)
      bg(1,724,kl) = RCONST(724)*VAR(177)*VAR(237)
      bg(1,725,kl) = RCONST(725)*VAR(177)*VAR(212)
      bg(1,726,kl) = RCONST(726)*VAR(153)*VAR(209)
      bg(1,727,kl) = RCONST(727)*VAR(192)*VAR(237)
      bg(1,728,kl) = RCONST(728)*VAR(192)*VAR(212)
      bg(1,729,kl) = RCONST(729)*VAR(192)*VAR(237)
      bg(1,730,kl) = RCONST(730)*VAR(192)*VAR(212)
      bg(1,731,kl) = RCONST(731)*VAR(192)*VAR(257)
      bg(1,732,kl) = RCONST(732)*VAR(192)*VAR(192)
      bg(1,733,kl) = RCONST(733)*VAR(144)*VAR(213)
      bg(1,734,kl) = RCONST(734)*VAR(144)*VAR(219)
      bg(1,735,kl) = RCONST(735)*VAR(89)*VAR(219)
      bg(1,736,kl) = RCONST(736)*VAR(64)*VAR(219)
      bg(1,737,kl) = RCONST(737)*VAR(166)*VAR(219)
      bg(1,738,kl) = RCONST(738)*VAR(190)*VAR(233)
      bg(1,739,kl) = RCONST(739)*VAR(190)*FIX(5)
      bg(1,740,kl) = RCONST(740)*VAR(190)*VAR(242)
      bg(1,741,kl) = RCONST(741)*VAR(115)*VAR(190)
      bg(1,742,kl) = RCONST(742)*VAR(190)*VAR(209)
      bg(1,743,kl) = RCONST(743)*VAR(190)*VAR(190)
      bg(1,744,kl) = RCONST(744)*VAR(219)*VAR(227)
      bg(1,745,kl) = RCONST(745)*VAR(213)*VAR(227)
      bg(1,746,kl) = RCONST(746)*VAR(153)*VAR(227)
      bg(1,747,kl) = RCONST(747)*VAR(183)*VAR(227)
      bg(1,748,kl) = RCONST(748)*VAR(177)*VAR(227)
      bg(1,749,kl) = RCONST(749)*VAR(130)*VAR(227)
      bg(1,750,kl) = RCONST(750)*VAR(227)*VAR(230)*VAR(254)
      bg(1,751,kl) = RCONST(751)*VAR(225)
      bg(1,752,kl) = RCONST(752)*VAR(219)*VAR(256)
      bg(1,753,kl) = RCONST(753)*VAR(228)*VAR(256)
      bg(1,754,kl) = RCONST(754)*VAR(247)*VAR(256)
      bg(1,755,kl) = RCONST(755)*VAR(256)*VAR(257)
      bg(1,756,kl) = RCONST(756)*VAR(233)*VAR(256)
      bg(1,757,kl) = RCONST(757)*VAR(141)*VAR(256)
      bg(1,758,kl) = RCONST(758)*VAR(197)*VAR(256)
      bg(1,759,kl) = RCONST(759)*VAR(115)*VAR(256)
      bg(1,760,kl) = RCONST(760)*VAR(237)*VAR(256)
      bg(1,761,kl) = RCONST(761)*VAR(212)*VAR(256)
      bg(1,762,kl) = RCONST(762)*VAR(256)*VAR(256)
      bg(1,763,kl) = RCONST(763)*VAR(190)*VAR(256)
      bg(1,764,kl) = RCONST(764)*VAR(144)*VAR(256)
      bg(1,765,kl) = RCONST(765)*VAR(74)
      bg(1,766,kl) = RCONST(766)*VAR(74)*VAR(230)
      bg(1,767,kl) = RCONST(767)*VAR(247)*VAR(254)
      bg(1,768,kl) = RCONST(768)*VAR(254)*VAR(257)
      bg(1,769,kl) = RCONST(769)*VAR(212)*VAR(254)
      bg(1,770,kl) = RCONST(770)*VAR(237)*VAR(254)
      bg(1,771,kl) = RCONST(771)*VAR(225)*VAR(247)
      bg(1,772,kl) = RCONST(772)*VAR(225)*VAR(257)
      bg(1,773,kl) = RCONST(773)*VAR(170)*VAR(228)
      bg(1,774,kl) = RCONST(774)*VAR(115)*VAR(170)
      bg(1,775,kl) = RCONST(775)*VAR(219)*VAR(236)
      bg(1,776,kl) = RCONST(776)*VAR(213)*VAR(236)
      bg(1,777,kl) = RCONST(777)*VAR(153)*VAR(236)
      bg(1,778,kl) = RCONST(778)*VAR(183)*VAR(236)
      bg(1,779,kl) = RCONST(779)*VAR(177)*VAR(236)
      bg(1,780,kl) = RCONST(780)*VAR(130)*VAR(236)
      bg(1,781,kl) = RCONST(781)*VAR(215)*VAR(230)*VAR(236)
      bg(1,782,kl) = RCONST(782)*VAR(167)
      bg(1,783,kl) = RCONST(783)*VAR(234)*VAR(257)
      bg(1,784,kl) = RCONST(784)*VAR(234)*VAR(247)
      bg(1,785,kl) = RCONST(785)*VAR(233)*VAR(234)
      bg(1,786,kl) = RCONST(786)*VAR(234)*VAR(234)
      bg(1,787,kl) = RCONST(787)*VAR(197)*VAR(234)
      bg(1,788,kl) = RCONST(788)*VAR(115)*VAR(234)
      bg(1,789,kl) = RCONST(789)*VAR(141)*VAR(234)
      bg(1,790,kl) = RCONST(790)*VAR(234)*VAR(237)
      bg(1,791,kl) = RCONST(791)*VAR(212)*VAR(234)
      bg(1,792,kl) = RCONST(792)*VAR(144)*VAR(234)
      bg(1,793,kl) = RCONST(793)*VAR(106)
      bg(1,794,kl) = RCONST(794)*VAR(106)
      bg(1,795,kl) = RCONST(795)*VAR(106)*VAR(230)
      bg(1,796,kl) = RCONST(796)*VAR(106)*VAR(236)
      bg(1,797,kl) = RCONST(797)*VAR(95)*VAR(212)
      bg(1,798,kl) = RCONST(798)*VAR(215)*VAR(247)
      bg(1,799,kl) = RCONST(799)*VAR(215)*VAR(257)
      bg(1,800,kl) = RCONST(800)*VAR(215)*VAR(233)
      bg(1,801,kl) = RCONST(801)*VAR(212)*VAR(215)
      bg(1,802,kl) = RCONST(802)*VAR(215)*VAR(237)
      bg(1,803,kl) = RCONST(803)*VAR(167)*VAR(247)
      bg(1,804,kl) = RCONST(804)*VAR(167)*VAR(257)
      bg(1,805,kl) = RCONST(805)*VAR(173)*VAR(206)*VAR(230)
      bg(1,806,kl) = RCONST(806)*VAR(205)
      bg(1,807,kl) = RCONST(807)*VAR(206)*VAR(227)*VAR(230)
      bg(1,808,kl) = RCONST(808)*VAR(132)
      bg(1,809,kl) = RCONST(809)*VAR(206)*VAR(230)*VAR(236)
      bg(1,810,kl) = RCONST(810)*VAR(204)
      bg(1,811,kl) = RCONST(811)*VAR(173)*VAR(230)*VAR(254)
      bg(1,812,kl) = RCONST(812)*VAR(173)*VAR(215)*VAR(230)
      bg(1,813,kl) = RCONST(813)*VAR(26)*VAR(26)
      bg(1,814,kl) = RCONST(814)*VAR(173)*VAR(213)
      bg(1,815,kl) = RCONST(815)*VAR(173)*VAR(204)
      bg(1,816,kl) = RCONST(816)*VAR(205)*VAR(236)
      bg(1,817,kl) = RCONST(817)*VAR(132)*VAR(173)
      bg(1,818,kl) = RCONST(818)*VAR(206)*VAR(225)
      bg(1,819,kl) = RCONST(819)*VAR(206)*VAR(237)
      bg(1,820,kl) = RCONST(820)*VAR(206)*VAR(212)
      bg(1,821,kl) = RCONST(821)*VAR(206)*VAR(254)
      bg(1,822,kl) = RCONST(822)*VAR(206)*VAR(215)
      bg(1,823,kl) = RCONST(823)*VAR(206)*VAR(206)
      bg(1,824,kl) = RCONST(824)*VAR(154)*VAR(173)*VAR(230)
      bg(1,825,kl) = RCONST(825)*VAR(154)*VAR(233)
      bg(1,826,kl) = RCONST(826)*VAR(154)*VAR(254)
      bg(1,827,kl) = RCONST(827)*VAR(154)*VAR(215)
      bg(1,828,kl) = RCONST(828)*VAR(154)*VAR(206)
      bg(1,829,kl) = RCONST(829)*VAR(122)*VAR(173)*VAR(230)
      bg(1,830,kl) = RCONST(830)*VAR(154)*VAR(205)
      bg(1,831,kl) = RCONST(831)*VAR(122)*VAR(173)*VAR(173)
      bg(1,832,kl) = RCONST(832)*VAR(205)*VAR(237)
      bg(1,833,kl) = RCONST(833)*VAR(230)*VAR(236)*VAR(254)
      bg(1,834,kl) = RCONST(834)*VAR(215)*VAR(227)*VAR(230)
      bg(1,835,kl) = RCONST(835)*VAR(145)
      bg(1,836,kl) = RCONST(836)*VAR(94)*VAR(230)*VAR(236)
      bg(1,837,kl) = RCONST(837)*VAR(225)*VAR(236)
      bg(1,838,kl) = RCONST(838)*VAR(72)
      bg(1,839,kl) = RCONST(839)*VAR(213)
      bg(1,840,kl) = RCONST(840)*VAR(233)
      bg(1,841,kl) = RCONST(841)*VAR(242)
      bg(1,842,kl) = RCONST(842)*VAR(141)
      bg(1,843,kl) = RCONST(843)*VAR(254)
      bg(1,844,kl) = RCONST(844)*VAR(225)
      bg(1,845,kl) = RCONST(845)*VAR(215)
      bg(1,846,kl) = RCONST(846)*VAR(167)
      bg(1,847,kl) = RCONST(847)*VAR(145)
      bg(1,848,kl) = RCONST(848)*FIX(1)
      bg(1,849,kl) = RCONST(849)*VAR(83)
      bg(1,850,kl) = RCONST(850)*VAR(208)
      bg(1,851,kl) = RCONST(851)*VAR(213)
      bg(1,852,kl) = RCONST(852)*VAR(231)
      bg(1,853,kl) = RCONST(853)*VAR(219)
      bg(1,854,kl) = RCONST(854)*VAR(210)
      bg(1,855,kl) = RCONST(855)*VAR(247)
      bg(1,856,kl) = RCONST(856)*VAR(134)
      bg(1,857,kl) = RCONST(857)*VAR(233)
      bg(1,858,kl) = RCONST(858)*VAR(58)
      bg(1,859,kl) = RCONST(859)*VAR(172)
      bg(1,860,kl) = RCONST(860)*VAR(207)
      bg(1,861,kl) = RCONST(861)*VAR(124)
      bg(1,862,kl) = RCONST(862)*VAR(198)
      bg(1,863,kl) = RCONST(863)*VAR(157)
      bg(1,864,kl) = RCONST(864)*VAR(189)
      bg(1,865,kl) = RCONST(865)*VAR(112)
      bg(1,866,kl) = RCONST(866)*VAR(88)
      bg(1,867,kl) = RCONST(867)*VAR(16)
      bg(1,868,kl) = RCONST(868)*VAR(184)
      bg(1,869,kl) = RCONST(869)*VAR(90)
      bg(1,870,kl) = RCONST(870)*VAR(105)
      bg(1,871,kl) = RCONST(871)*VAR(19)
      bg(1,872,kl) = RCONST(872)*VAR(151)
      bg(1,873,kl) = RCONST(873)*VAR(197)
      bg(1,874,kl) = RCONST(874)*VAR(211)
      bg(1,875,kl) = RCONST(875)*VAR(24)
      bg(1,876,kl) = RCONST(876)*VAR(251)
      bg(1,877,kl) = RCONST(877)*VAR(158)
      bg(1,878,kl) = RCONST(878)*VAR(229)
      bg(1,879,kl) = RCONST(879)*VAR(153)
      bg(1,880,kl) = RCONST(880)*VAR(81)
      bg(1,881,kl) = RCONST(881)*VAR(200)
      bg(1,882,kl) = RCONST(882)*VAR(187)
      bg(1,883,kl) = RCONST(883)*VAR(104)
      bg(1,884,kl) = RCONST(884)*VAR(102)
      bg(1,885,kl) = RCONST(885)*VAR(183)
      bg(1,886,kl) = RCONST(886)*VAR(77)
      bg(1,887,kl) = RCONST(887)*VAR(33)
      bg(1,888,kl) = RCONST(888)*VAR(169)
      bg(1,889,kl) = RCONST(889)*VAR(50)
      bg(1,890,kl) = RCONST(890)*VAR(40)
      bg(1,891,kl) = RCONST(891)*VAR(80)
      bg(1,892,kl) = RCONST(892)*VAR(195)
      bg(1,893,kl) = RCONST(893)*VAR(144)
      bg(1,894,kl) = RCONST(894)*VAR(185)
      bg(1,895,kl) = RCONST(895)*VAR(89)
      bg(1,896,kl) = RCONST(896)*VAR(18)
      bg(1,897,kl) = RCONST(897)*VAR(17)
      bg(1,898,kl) = RCONST(898)*VAR(178)
      bg(1,899,kl) = RCONST(899)*VAR(64)
      bg(1,900,kl) = RCONST(900)*VAR(113)
      bg(1,901,kl) = RCONST(901)*VAR(166)
      bg(1,902,kl) = RCONST(902)*VAR(59)
      bg(1,903,kl) = RCONST(903)*VAR(22)
      bg(1,904,kl) = RCONST(904)*VAR(163)
      bg(1,905,kl) = RCONST(905)*VAR(51)
      bg(1,906,kl) = RCONST(906)*VAR(160)
      bg(1,907,kl) = RCONST(907)*VAR(254)
      bg(1,908,kl) = RCONST(908)*VAR(133)
      bg(1,909,kl) = RCONST(909)*VAR(225)
      bg(1,910,kl) = RCONST(910)*VAR(164)
      bg(1,911,kl) = RCONST(911)*VAR(52)
      bg(1,912,kl) = RCONST(912)*VAR(150)
      bg(1,913,kl) = RCONST(913)*VAR(215)
      bg(1,914,kl) = RCONST(914)*VAR(221)
      bg(1,915,kl) = RCONST(915)*VAR(167)
      bg(1,916,kl) = RCONST(916)*VAR(202)
      bg(1,917,kl) = RCONST(917)*VAR(145)
      bg(1,918,kl) = RCONST(918)*VAR(98)
      bg(1,919,kl) = RCONST(919)*VAR(206)
      bg(1,920,kl) = RCONST(920)*VAR(246)
      bg(1,921,kl) = RCONST(921)*VAR(26)
      bg(1,922,kl) = RCONST(922)*VAR(175)
      bg(1,923,kl) = RCONST(923)*VAR(205)
      bg(1,924,kl) = RCONST(924)*VAR(252)
      bg(1,925,kl) = RCONST(925)*VAR(132)
      bg(1,926,kl) = RCONST(926)*VAR(135)
      bg(1,927,kl) = RCONST(927)*VAR(204)
      bg(1,928,kl) = RCONST(928)*VAR(21)
      bg(1,929,kl) = RCONST(929)*VAR(20)
      bg(1,930,kl) = RCONST(930)*FIX(5)
      bg(1,931,kl) = RCONST(931)*VAR(228)*VAR(230)
      bg(1,932,kl) = RCONST(932)*VAR(247)
      bg(1,933,kl) = RCONST(933)*VAR(230)*VAR(257)
      bg(1,934,kl) = RCONST(934)*VAR(112)
      bg(1,935,kl) = RCONST(935)*VAR(101)*VAR(230)
      bg(1,936,kl) = RCONST(936)*VAR(172)
      bg(1,937,kl) = RCONST(937)*VAR(100)*VAR(230)
      bg(1,938,kl) = RCONST(938)*VAR(200)
      bg(1,939,kl) = RCONST(939)*VAR(141)*VAR(230)
      bg(1,940,kl) = RCONST(940)*VAR(104)
      bg(1,941,kl) = RCONST(941)*VAR(230)*VAR(242)
      bg(1,942,kl) = RCONST(942)*VAR(183)
      bg(1,943,kl) = RCONST(943)*VAR(46)*VAR(230)
      bg(1,944,kl) = RCONST(944)*VAR(33)
      bg(1,945,kl) = RCONST(945)*VAR(32)*VAR(228)
      bg(1,946,kl) = RCONST(946)*VAR(237)
      bg(1,947,kl) = RCONST(947)*VAR(212)*VAR(230)
      bg(1,948,kl) = RCONST(948)*VAR(80)
      bg(1,949,kl) = RCONST(949)*VAR(165)*VAR(230)
      bg(1,950,kl) = RCONST(950)*VAR(165)
      bg(1,951,kl) = RCONST(951)*VAR(209)*VAR(230)
      bg(1,952,kl) = RCONST(952)*VAR(50)
      bg(1,953,kl) = RCONST(953)*VAR(230)*VAR(237)
      bg(1,954,kl) = RCONST(954)*VAR(51)
      bg(1,955,kl) = RCONST(955)*VAR(227)*VAR(230)
      bg(1,956,kl) = RCONST(956)*VAR(256)
      bg(1,957,kl) = RCONST(957)*VAR(190)*VAR(227)
      bg(1,958,kl) = RCONST(958)*VAR(254)
      bg(1,959,kl) = RCONST(959)*VAR(94)*VAR(230)
      bg(1,960,kl) = RCONST(960)*VAR(52)
      bg(1,961,kl) = RCONST(961)*VAR(230)*VAR(236)
      bg(1,962,kl) = RCONST(962)*VAR(234)
      bg(1,963,kl) = RCONST(963)*VAR(170)*VAR(236)
      bg(1,964,kl) = RCONST(964)*VAR(215)
      bg(1,965,kl) = RCONST(965)*VAR(95)*VAR(230)
      bg(1,966,kl) = RCONST(966)*VAR(145)*VAR(227)
      bg(1,967,kl) = RCONST(967)*VAR(72)
      bg(1,968,kl) = RCONST(968)*VAR(145)*VAR(236)
      bg(1,969,kl) = RCONST(969)*VAR(73)
      bg(1,970,kl) = RCONST(970)*VAR(167)*VAR(227)
      bg(1,971,kl) = RCONST(971)*VAR(73)
      bg(1,972,kl) = RCONST(972)*VAR(132)*VAR(227)
      bg(1,973,kl) = RCONST(973)*VAR(34)
      bg(1,974,kl) = RCONST(974)*VAR(204)*VAR(236)
      bg(1,975,kl) = RCONST(975)*VAR(35)
      bg(1,976,kl) = RCONST(976)*VAR(132)*VAR(236)
      bg(1,977,kl) = RCONST(977)*VAR(76)
      bg(1,978,kl) = RCONST(978)*VAR(204)*VAR(227)
      bg(1,979,kl) = RCONST(979)*VAR(76)
c accumulated rates (mlc/cm^3)
      do i=1,979
         bg(2,i,kl)=bg(2,i,kl)+dtg*bg(1,i,kl)
      end do
      return
      end
